{
  "name": "villa-saas-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd villa-saas && npm install",
    "build": "cd villa-saas && npm run build:backend",
    "start": "cd villa-saas/apps/backend && npm start"
  }
}