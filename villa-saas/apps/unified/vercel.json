{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install && cd packages/database && npx prisma generate && cd ../../apps/unified && npm install",
  "framework": "nextjs"
}